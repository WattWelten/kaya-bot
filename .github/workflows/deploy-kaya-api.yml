name: Deploy KAYA API to Railway

on:
  push:
    branches:
      - main
    paths:
      - 'kaya-api/**'
      - '.github/workflows/deploy-kaya-api.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Verify Railway GitHub Integration
        run: |
          echo "✅ Railway GitHub Integration sollte automatisch deployen"
          echo "📝 Push zu main Branch löst automatisch Deployment aus"
          echo "🔍 Prüfe Railway Dashboard für Deployment-Status"
          
          # Prüfe ob wichtige Dateien vorhanden sind
          if [ ! -f "kaya-api/Dockerfile" ]; then
            echo "❌ kaya-api/Dockerfile fehlt!"
            exit 1
          fi
          if [ ! -f "kaya-api/railway.toml" ]; then
            echo "❌ kaya-api/railway.toml fehlt!"
            exit 1
          fi
          
          echo "✅ Alle wichtigen Dateien vorhanden"
          echo "✅ Railway wird automatisch deployen (wenn GitHub Integration aktiv ist)"
