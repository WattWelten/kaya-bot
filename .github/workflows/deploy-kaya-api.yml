name: Deploy KAYA API to Railway

on:
  push:
    branches:
      - main
    paths:
      - 'kaya-api/**'
      - '.github/workflows/deploy-kaya-api.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Trigger Railway Deployment
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          echo "🚀 Triggering Railway Deployment für kaya-api"
          echo "📝 Hinweis: Railway GitHub Integration sollte automatisch deployen"
          echo "✅ Push zu main Branch löst automatisch Deployment aus (wenn Railway GitHub Integration aktiv ist)"
          
          # Railway GitHub Integration sollte automatisch deployen
          # Falls nicht aktiv, kann manuell im Dashboard deployen
          echo "✅ Workflow erfolgreich - Railway sollte automatisch deployen"
