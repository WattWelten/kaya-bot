name: Deploy KAYA Frontend to Railway

on:
  push:
    branches:
      - main
    paths:
      - 'kaya-frontend/**'
      - '.github/workflows/deploy-kaya-frontend.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install Railway CLI
        run: |
          curl -fsSL https://railway.app/install.sh | sh
          echo "$HOME/.railway/bin" >> $GITHUB_PATH
      
      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        working-directory: kaya-frontend
        run: |
          export PATH="$HOME/.railway/bin:$PATH"
          
          # Railway CLI mit expliziter Token-Konfiguration
          export RAILWAY_TOKEN="$RAILWAY_TOKEN"
          mkdir -p ~/.railway
          
          # Config-Datei mit korrektem Format erstellen
          echo "{\"token\":\"$RAILWAY_TOKEN\"}" > ~/.railway/config.json
          
          # Prüfe Railway CLI
          railway --version
          
          # Link zum Projekt und Service (ohne --non-interactive)
          railway link -p $RAILWAY_PROJECT_ID -s kaya-frontend
          
          # Deployment auslösen
          railway up --detach
          
          echo "✅ Deployment ausgelöst für kaya-frontend"

