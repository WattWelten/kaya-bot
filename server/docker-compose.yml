version: '3.8'

services:
  kaya-bot:
    build: .
    ports:
      - "3002:3002"
    volumes:
      - ./ki_backend:/app/ki_backend
    environment:
      - NODE_ENV=production
    restart: unless-stopped



